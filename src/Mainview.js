import React, { Component } from 'react';
import Repos from './Repos';

class Mainview extends Component {
    render() {
        return(
            <div>
                Click a repo on the right to get started
            </div>
        )
    }
}

export default Mainview;